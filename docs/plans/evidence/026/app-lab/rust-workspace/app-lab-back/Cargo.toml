[package]
name = "app-lab-back"
version = "0.1.0"
edition = "2021"

[dependencies]
libloading = "0.8"
autoterm-config = { path = "D:/autostack/auto-term/crates/autoterm-config" }

axum.workspace = true
tokio = { version = "1", features = ["full"] }
# Plan 617 T-05: ReaderStream for byte-range media streaming (no whole-file read).
tokio-util = { version = "0.7", features = ["io"] }
serde.workspace = true
serde_json.workspace = true
tower-http.workspace = true
auto-lang = { workspace = true, features = ["ui", "image-pipeline"] }
a2r-std = { workspace = true }
once_cell = "1"
