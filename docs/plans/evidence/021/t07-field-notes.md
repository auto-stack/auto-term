# PLAN-021 T-07 实机三联合场景——用户实测记录(2026-09-18)

载体:rust 轨 auto-term.exe(lang-021-p2 运行时,含 021 mouse_area
修复渲染器)+ 修复版 autoterm_core.dll(t07-rust-vehicle.png)。

## 实测结果(用户实点/实滚)

| # | 场景 | 结果 | 判读 |
|---|---|---|---|
| 1 | 滚动条出现 | 出了,但 thumb 太小且未对齐右边界(截图红框:thumb 落在内容区下方偏右,脱离面板右缘) | 自绘滚动条绘制/定位缺陷 |
| 2 | thumb 拖拽 | **基本可拖**(021 press/drag 链修复实机证实)但比例不对,无法拖到最早内容 | scrollbar_metrics 历史/行程映射缺陷 |
| 3 | 横分 | 右侧面板未出终端界面(蓝底卡死);分隔条未渲染,无法拖拽 | rust 轨分屏渲染缺陷(020 时代 VM 轨分屏正常;rust 轨分屏路径另有问题) |
| 4 | 滚轮 | 文字可滚,但只能回滚约半屏即卡住;滚动条不跟随 | wheel 子路径活;滚动钳位/比例缺陷 + thumb 不随 offset 重绘 |
| 5 | 接缝归属 | 分隔条未出,无法测 | 随 #3 延后 |

## 用户裁定(2026-09-18)

"自制滚动条问题多多,我们还是先就此打住,等实现了完整的 AutoUI
的官方滚动条(且支持虚拟滚动)再回来优化它。"

→ 与 T-06 裁定合并:自绘滚动条的度量/绘制/跟随缺陷清单 + 分屏
渲染问题,全部移交**官方滚动条(虚拟滚动)计划**时代处理;021 的
线 B 交付物维持"事件链修复"(thumb 可拖 + 滚轮可滚即链通实证)。
